# Normalize line endings
* text=auto

# Unreal asset files
*.uasset filter=lfs diff=lfs merge=lfs -text
*.umap  filter=lfs diff=lfs merge=lfs -text

# Optional: specific folders
Content/** filter=lfs diff=lfs merge=lfs -text